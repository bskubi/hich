FROM mambaorg/micromamba:latest

USER root
RUN micromamba install -c conda-forge -c bioconda -n base \
    git pip cooltools ucsc-bedgraphtobigwig cooler pairtools=1.1.0

ENV PATH="/opt/conda/bin:${PATH}"
RUN pip install git+https://github.com/open2c/MultiQC.git


CMD ["bash"]