[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hich"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
hich = "hich.cli:hich"